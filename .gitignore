node_modules/
dist/
build/

# Secrets — NEVER commit. service_role must stay server-side only.
.env
.env.*
!.env.example
!.env.production.example

# Local embedded-postgres data dirs (tests)
.pgdata*/

# Supabase CLI local files
supabase/.branches/
supabase/.temp/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Artefact genere par repomix (bundle du depot, non versionne)
repomix-output.xml

# Playwright browser artefacts (CI uploads them on failure).
playwright-report/
test-results/

# Manifeste de deploiement (instantane de release genere par `npm run manifest`)
deploy-manifest.json

# Outillage IA local (agents, configs, skills) — non versionne
.claude/
.codex/
.agents/
AGENTS.md
.vercel

# Sorties de commandes redirigees a la racine (AUD-2026-08-HYG-01) : elles ont
# deja contenu un dump de fichier source. Les ignorer evite qu'un `git add -A`
# les committe par accident.
/stdout
/stderr
/tsc_output.txt
